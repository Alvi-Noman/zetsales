# .gitignore
node_modules/
.pnpm-store/
.next/
dist/
build/
.turbo/
services/commerce-service/uploads/
.env
.env.*
.env*.local
!.env.example
*.timestamp-*.mjs
*.log
*.zip
.vscode/
.idea/
.DS_Store
*.ps1
# Local-only compose overrides (e.g. a local mongo container for dev) — must never reach the VPS,
# since docker compose auto-loads this file if present and that's exactly what exposed a mongo
# container publicly before.
docker-compose.override.yml
